{{ form_start(form, {
    'action': path('registration_ajax'),
    'attr': {
        'id': 'ajax-form-registration',
        'class': 'col p-0 my-3'
    }
}) }}

{% include 'flash_alert/_alert.html.twig' with {
    'alert_type': 'registration-success',
    'alert_style': 'success',
    'text_align': 'center'
} %}

<div class="form-group">
    {{ form_label(form.businessUsername) }}
    {{ form_widget(form.businessUsername) }}
</div>
<div class="form-group">
    {{ form_label(form.email) }}
    {{ form_widget(form.email) }}
</div>
<div class="form-group mb-0">
    {{ form_label(form.plainPassword.first) }}
    {{ form_widget(form.plainPassword.first) }}
    {% include 'component/_password_strength_meter.html.twig' %}
</div>
<div class="form-group">
    {{ form_label(form.plainPassword.second) }}
    {{ form_widget(form.plainPassword.second) }}
</div>

<div class="form-group">
    <input type="submit" class="btn btn-primary border-0 btn-block rounded-0 text-white"
           value="{{ 'user.register'|trans }}"
    />
</div>
{{ form_end(form) }}
