{{ form_start(form, {
    'action': path('account_information_edit_ajax'),
    'attr': {
        'id': 'ajax-form-account-information-edit',
        'class': 'col p-0 my-3'
    }
}) }}

{% include 'flash_alert/_alert.html.twig' with {
    'alert_type': 'account-information-edit-success',
    'alert_style': 'success',
    'text_align': 'center'
} %}

<div class="form-group">
    {{ form_label(form.businessUsername) }}
    {{ form_widget(form.businessUsername) }}
</div>

<div class="form-group">
    <input
            class="btn btn-primary border-0 rounded-0 text-white" type="submit"
            value="{{ 'global.form.save_changes'|trans }}"
    />
</div>
{{ form_end(form) }}
