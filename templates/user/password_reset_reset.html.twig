{% extends '_base.html.twig' %}

{% set page_title = 'page_title.password_reset'|trans %}

{% block body %}
    <div class="row">
        <h1 class="col">{{ 'page_title.password_reset'|trans }}</h1>
    </div>

    <div class="row mb-3">
        <div class="col">
            <a href="{{ path('home') }}">{{ 'page_title.home'|trans }}</a>
        </div>
    </div>

    {% include 'form/user/_password_reset.html.twig' %}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('password-strength-meter') }}
{% endblock %}
